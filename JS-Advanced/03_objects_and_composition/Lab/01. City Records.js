function solve(name, population, treasury) {
    const result = {
        name: name,
        population: population,
        treasury: treasury,
    };

    return result;
}

solve('Tortuga', 7000, 15000);
